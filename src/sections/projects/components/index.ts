// Projects Section Components
// Export individual components for use in the Projects section

export { ProjectsView } from '../ProjectsView';

// Re-export types for convenience
export type {
  Project,
  Folder,
  ProjectStats,
  SortOption,
  FilterOption,
  PlatformOption,
  ProjectsData,
  ViewMode,
  SortDirection,
  ProjectsViewState,
  BulkAction,
  ProjectActionHandlers,
  FolderActionHandlers,
  BulkActionHandlers,
  Platform,
  ProjectStatus,
} from '../../../../product/sections/projects/types';
