{
  "file_path": "src/types/index.ts",
  "main_branch_history": [],
  "task_views": {
    "025-add-typescript-type-system-documentation": {
      "task_id": "025-add-typescript-type-system-documentation",
      "branch_point": {
        "commit_hash": "ebdf6e38abd27054d01757c7f14894a93246a91b",
        "content": "export interface BannerElement {\n  id: string;\n  type: 'text' | 'image' | 'logo';\n  content: string; // text content or image url\n  x: number;\n  y: number;\n  width?: number;\n  height?: number;\n  rotation?: number; // degrees\n\n  // Typography\n  fontSize?: number;\n  fontWeight?: string; // '100' to '900'\n  fontFamily?: string;\n  fontStyle?: 'normal' | 'italic';\n  color?: string;\n  textAlign?: 'left' | 'center' | 'right';\n  letterSpacing?: number; // px (-10 to 50)\n  lineHeight?: number; // multiplier (0.8 to 3.0)\n\n  // Text transforms & decoration\n  textTransform?: 'none' | 'uppercase' | 'lowercase' | 'capitalize';\n  textDecoration?: 'none' | 'underline' | 'line-through' | 'underline line-through';\n\n  // Text background/highlight\n  backgroundColor?: string;\n  backgroundPadding?: number; // px (0-20)\n\n  // Opacity\n  opacity?: number; // 0-100\n\n  // Text shadow\n  textShadowColor?: string;\n  textShadowBlur?: number; // px (0-50)\n  textShadowOffsetX?: number; // px (-50 to 50)\n  textShadowOffsetY?: number; // px (-50 to 50)\n\n  // Text stroke/outline\n  textStrokeColor?: string;\n  textStrokeWidth?: number; // px (0-20)\n  textStrokeStyle?: 'solid' | 'dashed' | 'dotted';\n}\n\nexport interface ChatMessage {\n  role: 'user' | 'model';\n  text: string;\n  images?: string[]; // base64 strings\n  isThinking?: boolean;\n  groundingUrls?: Array<{ title: string; url: string }>;\n}\n\nexport interface LiveConnectionState {\n  isConnected: boolean;\n  isSpeaking: boolean;\n  error: string | null;\n}\n\nexport interface Part {\n  text?: string;\n  inlineData?: {\n    mimeType: string;\n    data: string;\n  };\n}\n",
        "timestamp": "2026-01-07T00:04:51.575410"
      },
      "worktree_state": {
        "content": "/**\n * @fileoverview UI component types for Nanobanna Pro\n *\n * This file contains TypeScript type definitions for UI-related functionality including:\n * - Canvas editor element types (BannerElement)\n * - Chat interface message types (ChatMessage)\n * - Voice agent connection state (LiveConnectionState)\n * - Gemini API message parts (Part)\n *\n * These types are used across the UI layer for component props, state management,\n * and user interaction handling.\n *\n * @see {@link ../context/CanvasContext.tsx} for canvas state management using BannerElement\n * @see {@link ../components/ChatInterface.tsx} for chat UI using ChatMessage\n * @see {@link ../components/LiveActionPanel.tsx} for voice agent UI using LiveConnectionState\n */\n\nexport interface BannerElement {\n  id: string;\n  type: 'text' | 'image' | 'logo';\n  content: string; // text content or image url\n  x: number;\n  y: number;\n  width?: number;\n  height?: number;\n  rotation?: number; // degrees\n\n  // Typography\n  fontSize?: number;\n  fontWeight?: string; // '100' to '900'\n  fontFamily?: string;\n  fontStyle?: 'normal' | 'italic';\n  color?: string;\n  textAlign?: 'left' | 'center' | 'right';\n  letterSpacing?: number; // px (-10 to 50)\n  lineHeight?: number; // multiplier (0.8 to 3.0)\n\n  // Text transforms & decoration\n  textTransform?: 'none' | 'uppercase' | 'lowercase' | 'capitalize';\n  textDecoration?: 'none' | 'underline' | 'line-through' | 'underline line-through';\n\n  // Text background/highlight\n  backgroundColor?: string;\n  backgroundPadding?: number; // px (0-20)\n\n  // Opacity\n  opacity?: number; // 0-100\n\n  // Text shadow\n  textShadowColor?: string;\n  textShadowBlur?: number; // px (0-50)\n  textShadowOffsetX?: number; // px (-50 to 50)\n  textShadowOffsetY?: number; // px (-50 to 50)\n\n  // Text stroke/outline\n  textStrokeColor?: string;\n  textStrokeWidth?: number; // px (0-20)\n  textStrokeStyle?: 'solid' | 'dashed' | 'dotted';\n}\n\nexport interface ChatMessage {\n  role: 'user' | 'model';\n  text: string;\n  images?: string[]; // base64 strings\n  isThinking?: boolean;\n  groundingUrls?: Array<{ title: string; url: string }>;\n}\n\nexport interface LiveConnectionState {\n  isConnected: boolean;\n  isSpeaking: boolean;\n  error: string | null;\n}\n\nexport interface Part {\n  text?: string;\n  inlineData?: {\n    mimeType: string;\n    data: string;\n  };\n}\n",
        "last_modified": "2026-01-07T00:04:52.039199"
      },
      "task_intent": {
        "title": "025-add-typescript-type-system-documentation",
        "description": "Types are split across multiple files (types/ai.ts, types/database.ts, types/index.ts, types/replicate.ts) without documentation explaining the organization, when to import from which file, or the relationship between similar types (e.g., ModelMetadata vs AIContextType, BrandProfile duplication).",
        "from_plan": true
      },
      "commits_behind_main": 7,
      "status": "active",
      "merged_at": null
    }
  },
  "created_at": "2026-01-07T00:04:51.879921",
  "last_updated": "2026-01-07T00:04:51.914935"
}